export enum EditorSidebar {
  form = "form",
  tableOfContent = "tableOfContent",
}

export enum ContentName {
  en = "enContent",
  ar = "arContent",
}

export enum TitleName {
  en = "enTitle",
  ar = "arTitle",
}
